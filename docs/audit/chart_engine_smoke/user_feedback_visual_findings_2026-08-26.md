# User feedback visual findings — 2026-08-26

The customer-facing technical caption was removed from the natal chart PDF block.

Kerykeion production rendering was switched from modern dark to classic dark, and the additional zodiac background ring was disabled. The resulting chart has one cleaner zodiac sign ring, a dark navy field that blends into the OracleAI page, and improved outer/inner-circle readability. No hand-built astrology geometry was introduced.

The “Карта в цифрах” page remains a two-panel composition: the left panel has a centered gold star core, birth profile strip, and placement tiles; the right panel has a separate Destiny Matrix panel. The page is materially less dense than the prior facts table.

The natal reference was split into separate calculation, placement, house-cusp, and aspect sections. The house-cusp section uses a dedicated page-break and larger 14pt table typography with increased row padding, so all 12 houses are no longer forced into the prior dense page.
