[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
# pytest-timeout: повесившийся тест режется через 2 минуты, а не висит до таймаута CI
timeout = 120
