# Основные зависимости зафиксированы точными версиями (G27): это «лок» —
# на них гоняются тесты и selfcheck, они ставятся в Docker и CI. Обновление —
# осознанный шаг: поднять версию, прогнать pytest, зафиксировать её здесь.
aiogram==3.30.0
aiosqlite==0.22.1
aiohttp==3.14.3
python-dotenv==1.2.2
fastapi==0.140.0
uvicorn[standard]==0.51.0
pydantic==2.13.4

# Мониторинг (G31): опционален — активен только когда задан SENTRY_DSN.
sentry-sdk>=2.0

# LLM-провайдеры. Нужен хотя бы один; без них продукт работает в офлайн-режиме
# (шаблонные трактовки на реальных расчётах карты, Таро и Матрицы).
anthropic==0.120.0
openai==2.48.0

# Астрология. Нужна именно 5.x: subject.model(), дома и NatalAspects.
kerykeion==5.12.9
geopy==2.5.0
timezonefinder==8.2.5

# Картинки-карточки для сторис. Без Pillow шеринг остаётся текстовым.
pillow==12.3.0

# PDF-разборы (товар для маркетплейсов). Диапазон сознательно: системные
# libcairo2/libpango ставятся в infra/Dockerfile; локально пакет опционален.
weasyprint>=62.0
