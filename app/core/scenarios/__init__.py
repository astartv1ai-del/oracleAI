"""Готовые сценарии генерации.

Каждый сценарий — фиксированная структура вывода, где детерминированные данные
(карты, планеты, арканы) выбираются кодом, а модель пишет текст. Это отличает
их от свободного диалога, который живёт в `app/core/agents/` — там модель сама
решает, какие инструменты позвать.

Модули:
    forecast    — дневной прогноз, карта дня, «сфера дня»
    tarot       — интерпретация конкретного расклада
    compat      — синастрия/совместимость
    report      — натальный отчёт + месячный разбор
    memory      — извлечение фактов из диалога в память

Реализация исторически была одним файлом (`app/core/agent.py`, 60 KB). Модули
ниже делят её по ответственностям и остаются canonical точкой входа. Тонкий
facade `app/core/agent.py` продолжает работать как public API для внешних
скриптов и планировщика.
"""
from . import compat, forecast, memory, report, tarot

__all__ = ["forecast", "tarot", "compat", "report", "memory"]
