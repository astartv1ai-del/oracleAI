"""Генератор персональных PDF-разборов — товар-витрина для маркетплейсов.

По ТЗ это «модуль №1»: канал продвижения без бюджета устроен так, что клиентка
сначала покупает PDF на площадке, а внутри файла находит «золотой билет» —
промокод на 30 дней VIP в боте. Канал сам по себе прибыльный, а бот получает
уже платившую аудиторию.

Модуль переиспользует ядро продукта: карту считает `core/astro`, арканы —
`core/matrix`, тексты пишет тот же агент по тем же правилам трактовки. Значит,
PDF и бот не могут разойтись в трактовках — а это первое, что заметит покупатель.

    builder.py — сбор данных и текстов разбора
    layout.py  — HTML+CSS страницы и SVG-колесо карты
    render.py  — HTML → PDF (WeasyPrint), с честным откатом в HTML
"""
from .builder import build_report_data, generate  # noqa: F401
from .render import render_pdf, to_pdf_bytes  # noqa: F401

__all__ = ["generate", "build_report_data", "render_pdf", "to_pdf_bytes"]
